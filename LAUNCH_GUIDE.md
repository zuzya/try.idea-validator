# 🚀 Памятка по запуску (Launch Guide)

## 1. Backend (Python API)
Открой первый терминал в корне проекта:

```bash
# Активация окружения (если нужно)
source .venv/bin/activate

# Запуск сервера
python -m uvicorn api:app --reload --port 8000
```

## 2. Frontend (React UI)
Открой второй терминал:

```bash
cd frontend
npm run dev
```

## 3. Открой в браузере
Перейди по ссылке: [http://localhost:5173](http://localhost:5173)
